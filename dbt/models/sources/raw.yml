version: 2

sources:
  - name: raw
    database: PLANNING_OS
    schema: RAW
    tables:
      - name: RAW_IOWA_LIQUOR
        config:
          loaded_at_field: loaded_at
          freshness:
            warn_after: { count: 7, period: day }
            error_after: { count: 14, period: day }
